var expect = require("chai").expect;

const User = require("../database/models/").User;

describe("Array", function() {
  before(function() {
    // User.create({
    //   firstName: "TestFn",
    //   lastName: "TestLn",
    //   email: "TestEmail1234"
    // });
  });

  it("should verify that we can create a new instance of the User class", function() {
    expect("true").to.equal("true");
  });

  it("should verify that we can create a new instance of the User class", function() {
    expect("true").to.equal("true");
  });

  it("should verify that we can create a new instance of the User class", function() {
    expect("true").to.equal("true");
  });

  it("should verify that we can create a new instance of the User class", function() {
    expect("true").to.equal("true");
  });
});
